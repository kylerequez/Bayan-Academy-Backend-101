<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head') %>
  <body>
    <%- include('partials/navbar') %>
    <h1>Your random number is <%= rand %></h1>
    <h1>Your name is <%= person.name %></h1>
    <h1>Your age is <%= person.age %></h1>
    <h1>
      Number is
      <!-- <% if(rand % 2 == 0 && rand != 0){ %>
            even
        <% } else if (rand != 0){ %>
            odd
        <% } else { %>
            zero
        <% } %> -->

      <%= rand % 2 == 0 && rand != 0 ? 'even' : rand != 0 ? 'odd' : 'zero'; %>
    </h1>
  </body>
</html>
