<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= category %> Products</title>
</head>

<body>
    <h1>
        <%= category %> Products</h1><a href="/products/new/">Add a new Product</a>
    <ul>
        <% for(let product of products){ %>
            <li>
                <%= product._id %> -
                    <%= product.name %> - P
                        <%= product.price %>
                            <a href="/products/<%= product._id %>">View</a>
                            <a href="/products/edit/<%= product._id %>">Edit</a>
            </li>
            <% } %>
    </ul>
</body>

</html>